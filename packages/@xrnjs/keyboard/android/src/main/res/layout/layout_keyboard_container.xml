<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:orientation="vertical"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="#00000000"
    android:layout_gravity="bottom">

    <xrn.modules.keyboard.inputmethod.KeyboardView
        android:id="@+id/v_keyboard"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@drawable/bg_keyboard"
        android:focusable="true"
        android:focusableInTouchMode="true"
        android:paddingLeft="4dp"
        android:paddingRight="4dp"
        android:paddingBottom="4dp"
        android:paddingTop="1dp"
        app:keyBackground="@drawable/selector_keyboard_key_background"
        app:shadowColor="#000000"
        app:keyTextColor="@color/keyboard_key_text_color"
        app:keyTextSize="@dimen/keyboard_text_size"
        app:labelTextSize="@dimen/keyboard_label_text_size"
        android:visibility="visible"/>

</LinearLayout>